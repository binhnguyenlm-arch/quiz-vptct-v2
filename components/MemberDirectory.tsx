 'use client';
import {useState} from 'react';
import {memberRoster} from '../lib/member-roster';
export default function MemberDirectory(){const [query,setQuery]=useState('');const clean=(s:string)=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').replace(/Đ/g,'D').toLowerCase();const rows=memberRoster.filter(p=>clean(p.name+' '+p.username).includes(clean(query.trim())));return <section className="memberDirectory" aria-labelledby="directory-title"><header><p>CHI BỘ VĂN PHÒNG TỔNG CÔNG TY</p><h2 id="directory-title">Tra cứu tài khoản đảng viên</h2><span>Chọn tên tài khoản tương ứng với họ tên của đồng chí. Mật khẩu được thông báo riêng.</span></header><label className="directorySearch">Tìm họ tên hoặc tên tài khoản<input type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Nhập họ tên hoặc tên tài khoản…"/></label><div className="directoryTableWrap"><table><thead><tr><th scope="col">STT</th><th scope="col">Đảng viên</th><th scope="col">Tên tài khoản</th></tr></thead><tbody>{rows.map(p=><tr key={p.username}><td>{p.number}</td><th scope="row">{p.name}</th><td><code>{p.username}</code></td></tr>)}</tbody></table></div>{rows.length===0&&<p className="directoryEmpty">Không tìm thấy tài khoản phù hợp.</p>}<p className="directoryFoot">{memberRoster.length} đảng viên · Nếu chưa đăng nhập được, vui lòng liên hệ quản trị viên để kiểm tra tài khoản.</p></section>;}
