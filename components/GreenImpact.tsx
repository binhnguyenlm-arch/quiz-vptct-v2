 'use client';
import {useEffect,useState} from 'react';
import WaterNumber from './WaterNumber';
import {loadStudyCounts,Counts} from './StudyCounts';
export default function GreenImpact(){const [green,setGreen]=useState<Counts['green']>(),[failed,setFailed]=useState(false);useEffect(()=>{let active=true;const refresh=()=>loadStudyCounts().then(d=>{if(active){setGreen(d.green);setFailed(!d.green);}}).catch(()=>{if(active)setFailed(true);});void refresh();const timer=setInterval(refresh,30000);return()=>{active=false;clearInterval(timer);};},[]);

return <section className="greenImpact" aria-labelledby="green-title"><header><h2 id="green-title">SỐ HÓA · XANH HÓA</h2><p>Học tập số · Văn phòng xanh</p></header><div className="greenMetrics"><div><WaterNumber value={green?.pages}/><h3>Trang tài liệu giấy không phải in ấn</h3><p>Ước tính qua lượt học tập hoàn thành</p></div><div><WaterNumber value={green?.documents}/><h3>Tài liệu đã số hóa</h3><p>Tích lũy từ khi triển khai</p></div></div><p className="greenScope">Ôn tập · Thi thử · Nghiên cứu nghị quyết</p>{failed?<p className="greenStatus" role="status">Chưa tải được thống kê. Vui lòng kiểm tra kết nối và cấu hình.</p>:!green?<p className="greenStatus" role="status">Đang tải số liệu…</p>:null}<details><summary>Cách tính số liệu</summary><p>Ước tính 3.000 ký tự/trang A4 cho câu hỏi và nội dung soạn thảo; PDF dùng số trang thực tế. Đây là lượng giấy tương đương nếu in cho mỗi lượt hoàn thành, không phải lượng giấy đã đo thực tế.</p><p>Tổng tài liệu gồm mỗi ngân hàng câu hỏi và mỗi tài liệu nghiên cứu được lưu trên hệ thống, kể cả tài liệu đã gỡ. Học lại nghị quyết không cộng trùng.</p>{green&&(green.unknownUsages>0||green.unknownDocuments>0)?<p className="greenMissing">Chưa quy đổi {green.unknownUsages.toLocaleString('vi-VN')} lượt/tài liệu đã học do thiếu số trang; {green.unknownDocuments.toLocaleString('vi-VN')} tài liệu chưa có thông tin trang. Ôn tập trước khi có bộ đếm không có dữ liệu để khôi phục.</p>:<p>Ôn tập chỉ tính từ khi hệ thống lưu chi tiết lượt hoàn thành.</p>}</details></section>;
}
